The Composite Design Pattern is a structural pattern used to treat a group of objects and individual objects uniformly. 
It allows clients to interact with both single objects and collections in the same way, 
forming a tree-like structure (like a file system).