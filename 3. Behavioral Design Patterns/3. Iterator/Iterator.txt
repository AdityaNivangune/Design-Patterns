The Iterator Pattern is a behavioral design pattern that provides a way to 
access elements of a collection sequentially without exposing its internal structure. 
It allows traversal without depending on the underlying data structure.